// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"insightstream/ent/feedaudittrailaction"
	"insightstream/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FeedAuditTrailActionQuery is the builder for querying FeedAuditTrailAction entities.
type FeedAuditTrailActionQuery struct {
	config
	ctx        *QueryContext
	order      []feedaudittrailaction.OrderOption
	inters     []Interceptor
	predicates []predicate.FeedAuditTrailAction
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FeedAuditTrailActionQuery builder.
func (fataq *FeedAuditTrailActionQuery) Where(ps ...predicate.FeedAuditTrailAction) *FeedAuditTrailActionQuery {
	fataq.predicates = append(fataq.predicates, ps...)
	return fataq
}

// Limit the number of records to be returned by this query.
func (fataq *FeedAuditTrailActionQuery) Limit(limit int) *FeedAuditTrailActionQuery {
	fataq.ctx.Limit = &limit
	return fataq
}

// Offset to start from.
func (fataq *FeedAuditTrailActionQuery) Offset(offset int) *FeedAuditTrailActionQuery {
	fataq.ctx.Offset = &offset
	return fataq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fataq *FeedAuditTrailActionQuery) Unique(unique bool) *FeedAuditTrailActionQuery {
	fataq.ctx.Unique = &unique
	return fataq
}

// Order specifies how the records should be ordered.
func (fataq *FeedAuditTrailActionQuery) Order(o ...feedaudittrailaction.OrderOption) *FeedAuditTrailActionQuery {
	fataq.order = append(fataq.order, o...)
	return fataq
}

// First returns the first FeedAuditTrailAction entity from the query.
// Returns a *NotFoundError when no FeedAuditTrailAction was found.
func (fataq *FeedAuditTrailActionQuery) First(ctx context.Context) (*FeedAuditTrailAction, error) {
	nodes, err := fataq.Limit(1).All(setContextOp(ctx, fataq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{feedaudittrailaction.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fataq *FeedAuditTrailActionQuery) FirstX(ctx context.Context) *FeedAuditTrailAction {
	node, err := fataq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FeedAuditTrailAction ID from the query.
// Returns a *NotFoundError when no FeedAuditTrailAction ID was found.
func (fataq *FeedAuditTrailActionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fataq.Limit(1).IDs(setContextOp(ctx, fataq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{feedaudittrailaction.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fataq *FeedAuditTrailActionQuery) FirstIDX(ctx context.Context) int {
	id, err := fataq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FeedAuditTrailAction entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FeedAuditTrailAction entity is found.
// Returns a *NotFoundError when no FeedAuditTrailAction entities are found.
func (fataq *FeedAuditTrailActionQuery) Only(ctx context.Context) (*FeedAuditTrailAction, error) {
	nodes, err := fataq.Limit(2).All(setContextOp(ctx, fataq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{feedaudittrailaction.Label}
	default:
		return nil, &NotSingularError{feedaudittrailaction.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fataq *FeedAuditTrailActionQuery) OnlyX(ctx context.Context) *FeedAuditTrailAction {
	node, err := fataq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FeedAuditTrailAction ID in the query.
// Returns a *NotSingularError when more than one FeedAuditTrailAction ID is found.
// Returns a *NotFoundError when no entities are found.
func (fataq *FeedAuditTrailActionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fataq.Limit(2).IDs(setContextOp(ctx, fataq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{feedaudittrailaction.Label}
	default:
		err = &NotSingularError{feedaudittrailaction.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fataq *FeedAuditTrailActionQuery) OnlyIDX(ctx context.Context) int {
	id, err := fataq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FeedAuditTrailActions.
func (fataq *FeedAuditTrailActionQuery) All(ctx context.Context) ([]*FeedAuditTrailAction, error) {
	ctx = setContextOp(ctx, fataq.ctx, ent.OpQueryAll)
	if err := fataq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FeedAuditTrailAction, *FeedAuditTrailActionQuery]()
	return withInterceptors[[]*FeedAuditTrailAction](ctx, fataq, qr, fataq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fataq *FeedAuditTrailActionQuery) AllX(ctx context.Context) []*FeedAuditTrailAction {
	nodes, err := fataq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FeedAuditTrailAction IDs.
func (fataq *FeedAuditTrailActionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if fataq.ctx.Unique == nil && fataq.path != nil {
		fataq.Unique(true)
	}
	ctx = setContextOp(ctx, fataq.ctx, ent.OpQueryIDs)
	if err = fataq.Select(feedaudittrailaction.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fataq *FeedAuditTrailActionQuery) IDsX(ctx context.Context) []int {
	ids, err := fataq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fataq *FeedAuditTrailActionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fataq.ctx, ent.OpQueryCount)
	if err := fataq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fataq, querierCount[*FeedAuditTrailActionQuery](), fataq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fataq *FeedAuditTrailActionQuery) CountX(ctx context.Context) int {
	count, err := fataq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fataq *FeedAuditTrailActionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fataq.ctx, ent.OpQueryExist)
	switch _, err := fataq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fataq *FeedAuditTrailActionQuery) ExistX(ctx context.Context) bool {
	exist, err := fataq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FeedAuditTrailActionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fataq *FeedAuditTrailActionQuery) Clone() *FeedAuditTrailActionQuery {
	if fataq == nil {
		return nil
	}
	return &FeedAuditTrailActionQuery{
		config:     fataq.config,
		ctx:        fataq.ctx.Clone(),
		order:      append([]feedaudittrailaction.OrderOption{}, fataq.order...),
		inters:     append([]Interceptor{}, fataq.inters...),
		predicates: append([]predicate.FeedAuditTrailAction{}, fataq.predicates...),
		// clone intermediate query.
		sql:  fataq.sql.Clone(),
		path: fataq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Action string `json:"action,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FeedAuditTrailAction.Query().
//		GroupBy(feedaudittrailaction.FieldAction).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fataq *FeedAuditTrailActionQuery) GroupBy(field string, fields ...string) *FeedAuditTrailActionGroupBy {
	fataq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FeedAuditTrailActionGroupBy{build: fataq}
	grbuild.flds = &fataq.ctx.Fields
	grbuild.label = feedaudittrailaction.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Action string `json:"action,omitempty"`
//	}
//
//	client.FeedAuditTrailAction.Query().
//		Select(feedaudittrailaction.FieldAction).
//		Scan(ctx, &v)
func (fataq *FeedAuditTrailActionQuery) Select(fields ...string) *FeedAuditTrailActionSelect {
	fataq.ctx.Fields = append(fataq.ctx.Fields, fields...)
	sbuild := &FeedAuditTrailActionSelect{FeedAuditTrailActionQuery: fataq}
	sbuild.label = feedaudittrailaction.Label
	sbuild.flds, sbuild.scan = &fataq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FeedAuditTrailActionSelect configured with the given aggregations.
func (fataq *FeedAuditTrailActionQuery) Aggregate(fns ...AggregateFunc) *FeedAuditTrailActionSelect {
	return fataq.Select().Aggregate(fns...)
}

func (fataq *FeedAuditTrailActionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fataq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fataq); err != nil {
				return err
			}
		}
	}
	for _, f := range fataq.ctx.Fields {
		if !feedaudittrailaction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fataq.path != nil {
		prev, err := fataq.path(ctx)
		if err != nil {
			return err
		}
		fataq.sql = prev
	}
	return nil
}

func (fataq *FeedAuditTrailActionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FeedAuditTrailAction, error) {
	var (
		nodes = []*FeedAuditTrailAction{}
		_spec = fataq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FeedAuditTrailAction).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FeedAuditTrailAction{config: fataq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fataq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fataq *FeedAuditTrailActionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fataq.querySpec()
	_spec.Node.Columns = fataq.ctx.Fields
	if len(fataq.ctx.Fields) > 0 {
		_spec.Unique = fataq.ctx.Unique != nil && *fataq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fataq.driver, _spec)
}

func (fataq *FeedAuditTrailActionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(feedaudittrailaction.Table, feedaudittrailaction.Columns, sqlgraph.NewFieldSpec(feedaudittrailaction.FieldID, field.TypeInt))
	_spec.From = fataq.sql
	if unique := fataq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fataq.path != nil {
		_spec.Unique = true
	}
	if fields := fataq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, feedaudittrailaction.FieldID)
		for i := range fields {
			if fields[i] != feedaudittrailaction.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fataq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fataq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fataq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fataq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fataq *FeedAuditTrailActionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fataq.driver.Dialect())
	t1 := builder.Table(feedaudittrailaction.Table)
	columns := fataq.ctx.Fields
	if len(columns) == 0 {
		columns = feedaudittrailaction.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fataq.sql != nil {
		selector = fataq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fataq.ctx.Unique != nil && *fataq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fataq.predicates {
		p(selector)
	}
	for _, p := range fataq.order {
		p(selector)
	}
	if offset := fataq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fataq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FeedAuditTrailActionGroupBy is the group-by builder for FeedAuditTrailAction entities.
type FeedAuditTrailActionGroupBy struct {
	selector
	build *FeedAuditTrailActionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fatagb *FeedAuditTrailActionGroupBy) Aggregate(fns ...AggregateFunc) *FeedAuditTrailActionGroupBy {
	fatagb.fns = append(fatagb.fns, fns...)
	return fatagb
}

// Scan applies the selector query and scans the result into the given value.
func (fatagb *FeedAuditTrailActionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fatagb.build.ctx, ent.OpQueryGroupBy)
	if err := fatagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FeedAuditTrailActionQuery, *FeedAuditTrailActionGroupBy](ctx, fatagb.build, fatagb, fatagb.build.inters, v)
}

func (fatagb *FeedAuditTrailActionGroupBy) sqlScan(ctx context.Context, root *FeedAuditTrailActionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fatagb.fns))
	for _, fn := range fatagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fatagb.flds)+len(fatagb.fns))
		for _, f := range *fatagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fatagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fatagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FeedAuditTrailActionSelect is the builder for selecting fields of FeedAuditTrailAction entities.
type FeedAuditTrailActionSelect struct {
	*FeedAuditTrailActionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fatas *FeedAuditTrailActionSelect) Aggregate(fns ...AggregateFunc) *FeedAuditTrailActionSelect {
	fatas.fns = append(fatas.fns, fns...)
	return fatas
}

// Scan applies the selector query and scans the result into the given value.
func (fatas *FeedAuditTrailActionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fatas.ctx, ent.OpQuerySelect)
	if err := fatas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FeedAuditTrailActionQuery, *FeedAuditTrailActionSelect](ctx, fatas.FeedAuditTrailActionQuery, fatas, fatas.inters, v)
}

func (fatas *FeedAuditTrailActionSelect) sqlScan(ctx context.Context, root *FeedAuditTrailActionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fatas.fns))
	for _, fn := range fatas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fatas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fatas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
